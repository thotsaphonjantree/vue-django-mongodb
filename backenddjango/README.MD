Enter the folder cloned and Setup the virtual environment (Windows): 
### `virtualenv -p python env` 
### `. ./env/Script/activate`

Install the dependencies: 
### `pip3 install -r requirement.txt`

Run server:
### `python manage.py runserver 8082`
